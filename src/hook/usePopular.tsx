import React from 'react';

interface PopularPros{
    id: number,
    category:string,
    title:string,
    image:string,
    views:number,
    likes:number,
    comments:number,
    createdAt:string,
}

const usePopular = () => {

    return (
        <div>
            
        </div>
    );
};

export default usePopular;